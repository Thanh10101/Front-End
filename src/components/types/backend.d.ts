interface IUser {
    id: number;
    firstName: string;
    lastName: string; // corrected typo from lastNaem to lastName
    address: string;
    email: string;
    phone: string;
    roleId: number;
    createdAt: string;
    updatedAt: string;
}